# 受入基準の充足状況

対象：要件定義書 v1.15 の AC-01〜AC-39、および v1.16 以降で追加された AC-40〜AC-43
作成日：2026年9月25日 ／ 最終更新：2026年9月25日

AC-40〜AC-43 はリポジトリの要件定義書（v1.15）にはまだ載っていない。本書では
指示された文面をそのまま条項として扱っている。

AC-38 が「掲げたが一部未実装」だったことを受けて、全件を洗い直した記録。

## 判定の基準

| 列 | 「済」と書ける条件 |
|---|---|
| 実装 | その条項を満たすコードが存在する |
| テスト | その条項を固定する自動テストが存在する。**テスト名に AC 番号があるだけでは足りず、条項そのものを検査していること** |
| 実機 | ブラウザで実際に操作して確認した |

- 3列すべてが埋まらないものは**未**と書く。推測で埋めない
- 複数の条項を持つ AC は条項ごとに判定する
- 実機確認が要らないと判断したものは、理由を備考に書く

---

## v1.0 対象（AC-01〜AC-10、AC-18、AC-20〜AC-30、AC-33〜AC-39）

| AC | 条項 | 実装 | テスト | 実機 | 備考 |
|---|---|---|---|---|---|
| AC-01 | 利用日に口座残高が動かない | 済 | 済 | 済 | `cash.test.ts`。ダッシュボードのカード未払で確認 |
| AC-01 | 引落日に10,000円が落ちる | 済 | 済 | 済 | `balance.test.ts` に通しの確認あり |
| AC-02 | day=31 が2月は28/29日 | 済 | 済 | 未 | `forecast.test.ts`。**純関数の日付計算で、UIに分岐が無い。実機確認は不要と判断** |
| AC-02 | day=31 が4月は30日 | 済 | 済 | 未 | 同上 |
| AC-03 | 消し込むと予定が予測から消える | 済 | 済 | 済 | `mutations.test.ts`。要対応リストで確認 |
| AC-03 | 実績残高に反映される | 済 | 済 | 済 | ヘッダーの残高が動くことを確認 |
| AC-03 | 年月別収支の「予定」列が変わらない | 済 | 済 | 済 | 手計算と突合。消し込み前後で予定列は 129,800／365,200 のまま、実績列だけが 223,000／254,000 に変化 |
| AC-04 | 日付をずらすと残高推移が変わる | 済 | 済 | 済 | `override-deferral.test.ts`。繰延で確認 |
| AC-04 | 年月別収支の予定額も移動する | 済 | 済 | 済 | 9/20→10/20 の繰延で、9月の固定費 129,800→29,800、10月 129,800→229,800。手計算と一致 |
| AC-05 | 事業/家計/合算の3ビューで按分 | 済 | 済 | 済 | 仕様の例そのままの通信費9,800・事業40%で、画面が 合算9,800／事業3,920／家計5,880 を表示 |
| AC-06 | 月中に防衛ライン割れの月に警告 | 済 | 済 | 済 | `monthly.test.ts`。月次資金繰り表で確認 |
| AC-07 | Shift_JIS を読む | 済 | 済 | **未** | `csv.test.ts`。**実機では UTF-8 の貼り付けしか試していない** |
| AC-07 | 4列を自動推定する | 済 | 済 | 済 | 取込画面で日付・摘要・入金・出金が当たることを確認 |
| AC-07 | 金額一致する予定が自動照合される | 済 | 済 | 済 | AC-33 の確認時に消し込み先が付くことを確認 |
| AC-08 | 6ヶ月・固定費のみ・予定のみで絞れる | 済 | 済 | 済 | `ledger.test.ts`。キャッシュフロー画面で確認 |
| AC-08 | ヘッダの合計が絞り込み後の値になる | 済 | 済 | 済 | 同上 |
| AC-09 | 375px で全画面が操作可能 | 済 | — | 済 | 6画面すべて（/ /cashflow /pl /entry /import /settings）で横スクロールなしを確認。**自動テストは無い（DOM 環境が無いため）** |
| AC-09 | テーブルが横スクロールで参照できる | 済 | — | 済 | 各表を `overflow-x-auto` で包んでいる |
| AC-10 | 閉じて再度開いてもデータが残る | 済 | — | 済 | 再読込での保持を多数回確認。**「閉じて開く」ではなく再読込で代用している** |
| AC-18 | 同一費目を固定費と変動費の両方に出す | 済 | 済 | 済 | 水道光熱費が固定費20,000・変動費5,000 の2行で出ることを画面で確認 |
| AC-20 | TRF が年月別収支に現れない | 済 | 済 | 済 | TRF-01 資金移動 30,000 を予定・実績の両方に入れたうえで、6通りの表示すべてに1行も出ないことを確認 |
| AC-20 | TRF が月次資金繰り表の出金に含まれる | 済 | 済 | **未** | `monthly.test.ts`・`balance.test.ts`。月次資金繰り表の画面確認は未 |
| AC-21 | `months: []` が発生しない | 済 | 済 | 済 | `forecast.test.ts`。全角「６」等で確認 |
| AC-21 | 空配列が保存されない | 済 | 済 | 済 | `number-field.test.ts`。誤入力で直前の値が守られることを確認 |
| AC-22 | 既定が「先月〜6ヶ月先」 | 済 | 済 | 済 | `ledger.test.ts`。2026年8月〜2027年2月を確認 |
| AC-22 | 先月が基準日より前なら基準日の月に寄せる | 済 | 済 | 済 | 基準日を当月にして確認 |
| AC-23 | 未消込の予定を実績と区別して強調 | 済 | 済 | 済 | `ledger.test.ts`。桃色の行と「未入力」を確認 |
| AC-23 | 月グループ見出しに未入力件数 | 済 | 済 | 済 | 「未入力 1件」を確認 |
| AC-24 | 金額を変えても消し込みが外れない | 済 | 済 | 済 | `mutations.test.ts`。7,700 に変更して確認 |
| AC-25 | 日付を変えても消し込みが外れない | 済 | 済 | 済 | 9/17→9/19 に変更して確認 |
| AC-26 | 費目を直すと年月別収支に反映 | 済 | 済 | 済 | 雑費→地代家賃で年月別収支の行が動くことを確認 |
| AC-27 | 「最近の実績」から編集に入れる | 済 | — | 済 | **自動テストは無い（UI の導線）** |
| AC-27 | 入出金予定表の実績行から編集に入れる | 済 | — | 済 | 同上 |
| AC-28a | 総件数すべてに操作だけで到達できる | 済 | 済 | 済 | `actual-list.test.ts`。547件を辿って 200+150+197 を確認 |
| AC-28a | 重複がない | 済 | 済 | 済 | 同上 |
| **AC-28b** | 10万件で初期表示が §5.1 を満たす | — | — | **未** | **負荷試験アカウント待ち。未測定** |
| AC-29a | 45日で「止まっている」警告 | 済 | 済 | 済 | `reconcile-warnings.test.ts`。115日で確認 |
| AC-29a | null なら基準日を起点 | 済 | 済 | 済 | 同上 |
| AC-29a | 新規利用者が初日に警告を受けない | 済 | 済 | **未** | テストで固定。**実機では基準日=今日の状態を作っていない** |
| AC-29b | 60日以上の未消込で警告 | 済 | 済 | 済 | 3件中1件だけが数えられることを確認（59日が除外） |
| AC-29b | 未到来の未消込を数えない | 済 | 済 | 済 | 同上 |
| AC-29c | null のとき「まだ一度も行われていない」 | 済 | 済 | 済 | 文面を確認。「止まっています」が出ないことも確認 |
| AC-30 | 候補が提示される | 済 | 済 | 済 | `reconcile.test.ts`。実機で確認 |
| AC-30 | 確定すると残高から予定額が消える | 済 | 済 | 済 | 30日後が +9,800 されることを確認 |
| AC-30 | 確定しない限り二重計上のまま | 済 | 済 | 済 | テストで固定。実機でも残高が低いままを確認 |
| AC-33 | 選んだ口座の予定だけが候補になる | 済 | 済 | 済 | `csv.test.ts`。並び順で先頭に来ない側が当たることを確認 |
| AC-34 | 上限超でも件数が一致する | 済 | 済 | **未** | `fetch-all.test.ts`（偽サーバー）。**実サーバーで上限を超える件数は未検証** |
| AC-34 | 重複と欠落がない | 済 | 済 | **未** | 同上 |
| AC-34 | 全テーブルで並び替えキーが一意 | 済 | 済 | 済 | 実リクエストで `order=date.asc,id.asc` 等を確認 |
| AC-35 | 不一致で残高が表示されない | 済 | 済 | 済 | 読み込み失敗時に画面を出さないことを実機で確認 |
| AC-35 | 読み込み失敗が画面に示される | 済 | 済 | 済 | 同上。再試行ボタンも確認 |
| AC-35 | 部分データで計算した残高を出さない | 済 | 済 | 済 | 同上 |
| AC-36 | 確定金額が最初の引落日 | 済 | 済 | 済 | `cash.test.ts`。9/27 −100,000 を確認 |
| AC-36 | 未確定分がその次の引落日 | 済 | 済 | 済 | 10/27 −60,000 を確認 |
| AC-36 | 両者が同じ引落日にまとまらない | 済 | 済 | 済 | 同上 |
| AC-37 | 1つのリストに予定日の昇順で並ぶ | 済 | 済 | 済 | `todo-list.test.ts`。9/26 候補・9/28 予定を確認 |
| AC-37 | 2つが別リストに分かれていない | 済 | 済 | 済 | 同上 |
| AC-38 | 再読込しても候補が再提示されない | 済 | 済 | **未** | **0006 未適用のため保存できず、再読込での確認ができていない** |
| AC-38 | 要対応の件数から除かれる | 済 | 済 | 済 | 2件→1件を確認 |
| AC-38 | 実績の編集から取り消せる | 済 | 済 | 済 | 取り消して候補が戻ることを確認 |
| AC-39 | 候補が全件提示される | 済 | 済 | 済 | `reconcile.test.ts`。実機で「候補 2件」とボタン2つを確認 |
| AC-40 | 費用の差異が `予定 − 実績` | 済 | 済 | 済 | 手計算と突合。地代家賃 100,000−120,000＝−20,000、通信費 9,800−7,000＝+2,800 |
| AC-40 | 収入の差異が `実績 − 予定` | 済 | 済 | 済 | 事業売上 480,000−500,000＝−20,000、雑収入 55,000−30,000＝+25,000 |
| AC-40 | 同一月に両方向を置いて符号が正しい | 済 | 済 | 済 | 2026-09 に 実績>予定 の費目3件と 実績<予定 の費目3件を同居させ、11行すべてが手計算と一致 |
| AC-40 | 正の値が良い方向の色 | 済 | 済 | 済 | 正が `object-success-bright`（rgb 6,136,145）、負が `object-error-dim`（rgb 99,6,62）。要件定義書の「緑」は誤記で、**このまま（success 色）が正しいことを確認済み** |
| AC-41 | 過去月は実績、未来月は予定 | 済 | 済 | 済 | 10月に予定100,000と実績125,000を同居させ、`実績+予定` が予定100,000を出すことを確認 |
| AC-41 | 境界（当月）の扱いが定義どおり | 済 | 済 | **未** | `実績+予定` は当月を実績側として扱う。**「定義どおり」を判定できる定義が仕様に無い（下記）。** 差異モード側は AC-42 で解消した |
| AC-42 | 予定日が本日以前の予定のみが予定側 | 済 | 済 | 済 | `planToDate`。9/25 時点で 9/20 の予定は入り 9/30 の予定は入らないことをテストと画面で確認 |
| AC-42 | 当月の未到来の予定が差異に含まれない | 済 | 済 | 済 | 9/30 の予定30,000 を置いた状態で、9月の固定費差異が +7,800 から **−22,200** に、収支が +14,800 から **−15,200** に戻った |
| AC-42 | 未来月が空になるのが規則から導かれている | 済 | 済 | 済 | `if (!isPast) return null` を削除。11月・12月は比べる予定も実績も無いので0になる。**10月は未来日の実績125,000があるため −125,000 と出る**（下記） |
| AC-43 | 当月の差異の列に本日の注記 | 済 | 済 | 済 | 差異モードでのみ 9月の列見出しに「9/25 時点」。ほかのモードでは出ない |

## v1.5 対象（実装しない）

| AC | 内容 | 状態 |
|---|---|---|
| AC-31 | 取引先×月マトリクスの入金が残高に現れる | **対象外（v1.5）** |
| AC-32 | 取引先未設定の収入が「未設定」行に出る | **対象外（v1.5）** |

## v2.0 対象（実装しない）

| AC | 内容 | 状態 |
|---|---|---|
| AC-11 | 借入返済スケジュール（据置・元利均等） | **対象外（v2.0）**。`v2-acceptance.test.ts` に `it.todo` で置いてある |
| AC-12 | 元金と利息の分離計上 | 対象外（v2.0）。同上 |
| AC-13 | 入金サイトからの入金予定 | 対象外（v2.0）。同上 |
| AC-14 | 入金遅延の反映 | 対象外（v2.0）。同上 |
| AC-15 | 請求の消し込み | 対象外（v2.0）。同上 |
| AC-16 | 税理士事務所への代理操作権限 | 対象外（v2.0）。同上 |
| AC-17 | 代理操作の記録 | 対象外（v2.0）。同上 |
| AC-19 | 借入返済1回の2件への分解 | 対象外（v2.0）。**後半（v1.0 で検証できる範囲）は `pl.test.ts` で済** |

---

## 未になっている項目のまとめ

### 実機確認が残っているもの

年月別収支（/pl）は手計算と突合済み。**予定・実績の2モード × 3スコープ**（AC-03/04/05/18/20）と、
**4モード × 合算スコープ**（AC-40/41）を読んだ。4モード × 3スコープの全12通りは読んでいない。

| AC | 何が未確認か |
|---|---|
| AC-20 | 月次資金繰り表の画面で TRF が出金に含まれること（年月別収支側は確認済み） |
| AC-07 | Shift_JIS の実ファイル取込 |
| AC-29a | 新規利用者（基準日=今日）が警告を受けないこと |
| AC-34 | 実サーバーで返却上限を超える件数の取得 |
| AC-38 | 再読込しても候補が再提示されないこと |
| AC-41 | 当月の扱い。**判定できる定義が仕様に無い**（下記） |

### 測定していないもの（1件）

| AC | 状態 |
|---|---|
| AC-28b | 実績10万件での初期表示。**負荷試験アカウント待ち** |

### 自動テストが無いもの（3条項）

いずれも UI の導線。jsdom は CLAUDE.md §2.7 の許可リストに入ったが、**レイアウトは jsdom で検証できない**（レイアウトエンジンが無く、`scrollWidth` が実際の値を返さない）。

- AC-09（375px、横スクロール）— jsdom では自動化しない。実機で確認する
- AC-10（再読込での保持）— jsdom で自動化できる可能性がある
- AC-27（2つの入口から編集に入れる）— 同上

## 進め方の提案

AC-34 と AC-38 は**マイグレーション適用と負荷試験で同時に潰せます**（0006 が入れば AC-38 の再読込が確認でき、10万件を入れれば AC-34 の実サーバー検証ができます）。

残るのは Shift_JIS、AC-29a、月次資金繰り表の TRF の3件で、**モニター配布前に1回ブラウザを通せば片付きます。**

---

## 確認中に見つけたこと

### 「実績」モードが未来月の実績を表示しない（修正済み）

`applyMode`（`src/core/pl.ts`）が `case "actual": return isPast ? actual : null;` になっており、今月より後の月にある実績が「実績」モードで消えていた。

CL-5 手順3（実績側は実績のみを集計する）には期間の限定が無い。過去／未来の切り分けは手順6 の `実績+予定` の定義に属するものであり、それが `実績` に漏れていた。**仕様違反として `return actual;` に修正した。**

未来日の実績は実在する。前払い、CSVの先日付行、そして最も多いのは**日付の打ち間違い**である。2027年と打ってしまった実績が画面から消えると、残高だけが合わない状態になって原因が追えない。

### 差異の比較相手を「予定日が本日以前の予定」にした（AC-42、修正済み）

差異モードには、仕様に無い期間の分岐が2つあった。

- `case "diff"` の `if (!isPast) return null;`（未来月を隠す）
- 当月を実績側に置く扱い（当月の未到来の予定が予定側に丸ごと残る）

どちらも「差異は実績が出揃っていない期間では意味を持たない」という同じ事実への場当たりの対処だった。**規則を1つ置いて両方削除した。**

> 差異は、予定日が本日以前の予定インスタンスと実績を比べる。

集計日は発生日（カードは利用日。手順4）なので、判定も発生日で行う。実装は `PLMatrix.planToDate` として予定側をもう1面持ち、差異モードだけがそれを見る。`予定` モードと `実績+予定` モードは従来どおり全予定を見る（手順2の「当初予算を保つ」）。

修正前後を実機で比べた（本日 2026-09-25、9/30 に予定30,000）。

| 行 | 修正前 | 修正後 |
|---|---|---|
| 租税公課（9/30・未到来） | +30,000 | **0** |
| 固定費計 | +7,800 | **−22,200** |
| 収支 | **+14,800（良い方向の色）** | **−15,200（悪い方向の色）** |

3費目で予算を超えている月が、良い方向に見えなくなった。

**未来月が空になるのは規則から導かれている。** 11月・12月は比べる予定も実績も無いので0になる。期間で切ってはいない。

### 未来日の実績がある月は、差異が予定超過として出る

上の規則の帰結として、**未来月に実績だけがあると、比べる予定が無いので全額が予定超過（負）として出る。** 実機では10月に未来日の実績125,000を置いてあり、10月の固定費差異が −125,000 になっている。

未来日の実績の多くは日付の打ち間違いなので、負の差異として目立つほうが見つけやすい。意図と違うなら仕様で定義してほしい。

### `実績+予定` の当月の扱いが仕様で決まっていない（判断が要る）

手順6 は「過去月は実績、未来月は予定」としか書いておらず、**当月がどちらかを決めていない。** 実装は `月 <= 今月` で判定しているので、当月は実績側になる。

今日 2026-09-25 の状態で 9/30 に予定30,000（固定資産税）を置いて `実績+予定` を開くと、9月の固定費は**実績の 152,000** になり、まだ到来していない 30,000 は金額として出ない。ただし予定額の注記「予 159,800」には出るため、隠れてはいない。

差異モード側は AC-42 で解消した。残っているのは `実績+予定` の当月をどちら側に置くかという定義だけで、**実害は確認できていない。**
