import { ScreenPlaceholder } from "@/components/app-shell/ScreenPlaceholder";

export default function CashflowPage() {
  return (
    <ScreenPlaceholder
      screenId="SC-03"
      title="キャッシュフロー"
      summary="残高推移グラフ（日次／月次切替）、月次資金繰り表、入出金予定表。"
      task="フェーズ2・タスク#8（FR-11, FR-12）"
    />
  );
}
