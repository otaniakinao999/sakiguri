# CLAUDE.md — Sakiguri 開発規約

このファイルは Claude Code が毎回読む前提の規約です。ここに書かれたことは推測で上書きしないでください。

## 1. このプロダクトは何か

個人事業主と小規模法人のための資金繰り予測サービス。予定を先に登録し、実績で消し込みながら、日次の残高予測を最新に保つ。

**仕様の一次情報は `docs/要件定義書.md`。** 特に以下を必ず参照する。

- `3.2 データモデル` — エンティティと項目
- `3.3 計算ロジック仕様` — CL-1〜CL-9。実装はここに従う
- `9. 受入基準` — AC-01〜AC-17。テストの元ネタ

**仕様に書かれていないことを実装しないでください。** 迷ったら実装せず、何が決まっていないかを質問してください。推測で仕様を作ると、後から食い違いを見つけるのが極めて難しくなります。

## 2. 絶対に守る規約

### 2.1 金額は円単位の整数

```ts
// OK
type Yen = number;              // 整数。小数点以下を持たない
const total: Yen = 120_000;
const share = Math.round(amount * ratio / 100);

// NG
const total = 120000.5;         // 小数を持たせない
const share = amount * 0.25;    // 丸めずに次の計算へ渡さない
```

按分・利息・端数はすべて計算の各段で `Math.round` する。累積誤差が出る箇所（借入返済表）は最終回で残額を調整し、**合計が元本と一致することをテストで保証する**（AC-11）。

浮動小数点の比較に `===` を使わない。金額の一致判定は整数化した後に行う。

### 2.2 日付は 'YYYY-MM-DD' の文字列で持つ

```ts
type DateStr = string;          // 'YYYY-MM-DD' 固定

// OK
const ymd = (d: Date) => `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
if (a <= b) { ... }             // 文字列比較で日付の前後が判定できる

// NG
new Date().toISOString()        // UTC に変換されて日付がずれる
d.getTime() - e.getTime()       // タイムゾーンに依存する比較
```

`Date` オブジェクトは月末計算などの内側だけで使い、境界では必ず文字列に戻す。**UTC 変換を伴う API を使わない。** サーバー側でも同じ規約に従う。JST 固定で扱い、タイムゾーンを設定項目にしない。

### 2.3 `src/core/` は純関数のみ

```
src/core/          ← 計算ロジック。React / DOM / fetch / Date.now() を import しない
  forecast.ts      CL-1  予定インスタンスの展開
  cash.ts          CL-2  現金イベント化（カード引落ラグ）
  balance.ts       CL-3  残高推移
  proration.ts     CL-4  家事按分
  pl.ts            CL-5  年月別収支
  monthly.ts       CL-6  月次資金繰り表
  csv.ts           CL-7  CSV 解析と照合
  loan.ts          CL-8  借入返済スケジュール
  receivable.ts    CL-9  売掛金の入金予定
  __tests__/       受入基準 AC-01〜17 に対応するテスト
```

`core` は「入力を渡すと同じ出力が返る」ことを保証する。現在日時は引数で受け取る（`today: DateStr`）。`new Date()` を core の中で呼ばない。これを守ると、テストが書けて、AI が壊したときに検知できる。

### 2.4 core を変更したらテストを更新する

**テストが落ちたまま次のタスクに進まないでください。** 計算ロジックは「間違っていても動く」ため、テストがない変更は静かに壊れます。

新しい計算規則を追加したら、対応するテストも同じコミットに含める。既存テストを「実装に合わせて」書き換える前に、仕様がどちらなのかを確認してください。

### 2.5 Supabase のテーブルを作ったら必ず RLS を有効化する

```sql
alter table public.actuals enable row level security;

create policy "own rows" on public.actuals
  for all using (auth.uid() = user_id);
```

**RLS のないテーブルを作らないでください。** 他人の資金繰りが見える事故は、このプロダクトでは致命的です。マイグレーションを書いたら、RLS のポリシーが漏れていないかを自分で確認してからレビューに出してください。

### 2.6 色とサイズはトークンから引く

`src/styles/tokens.css` の CSS 変数のみを使う。

```css
/* OK */
color: var(--object-error-dim);
padding: var(--sp-16);

/* NG */
color: #63063E;
padding: 15px;        /* スケール外の値を作らない */
```

余白は 4 / 8 / 12 / 16 / 24 / 32 / 40 / 48 のスケールのみ。中間値を作らない。トークンの定義は `docs/designsystem.md` にある。

### 2.7 依存を増やす前に確認する

許可済み: `react` `next` `typescript` `tailwindcss` `recharts` `@supabase/supabase-js` `zod` `vitest` `@testing-library/react`

これ以外のライブラリを追加したいときは、**入れる前に理由と代替案を提示してください。** 特に日付ライブラリ（dayjs / date-fns）は 2.2 の規約と競合するため入れません。

## 3. タスクの進め方

1つの作業単位は1つのPRにする。PRの説明には必ず要件定義書の FR 番号を書く。

```
feat(core): CL-8 借入返済スケジュールの生成 [FR-24, FR-25]

- 元利均等・元金均等・期日一括に対応
- 据置期間中は利息のみ
- 元金と利息を分離して返す（資金繰りと損益で扱いが違う）
- AC-11, AC-12 のテストを追加
```

**大きなタスクを勝手に分割・統合しないでください。** タスクの粒度に疑問があるときは着手前に言ってください。

## 4. 設計判断を記録する

`docs/adr/` に1判断1ファイルで残す。

```
docs/adr/0001-金額を円単位の整数で扱う.md
docs/adr/0002-日付を文字列で持つ.md
docs/adr/0003-残高計算をクライアント側で行う.md
```

形式は「文脈 / 判断 / 理由 / 却下した選択肢 / 影響」。**AIが書いたコードは、誰も理由を説明できない状態になりやすい。** ADR はそれを防ぐためにあり、将来の事業譲渡時の資産にもなる。

新しい設計判断をしたときは、コードと同じPRで ADR を追加してください。

## 5. やらないこと

- 銀行・カードの API 連携（CSV取込のみ。電子決済等代行業の登録対象外に留める）
- 口座番号、カード番号、金融機関の認証情報の保存
- CSVファイル本体のサーバー保存（解析結果のみ保存する）
- 仕訳、決算書、確定申告書の作成
- 税理士事務所向けの一括管理画面・顧問先一覧・監視機能
- 承認フロー、部門別予算、監査ログ、SSO

これらは意図して範囲外にしています。「あると便利だから」で追加しないでください。

## 6. コマンド

```bash
pnpm dev            # 開発サーバー
pnpm test           # Vitest（core のテスト）
pnpm test:watch
pnpm typecheck      # tsc --noEmit
pnpm lint
pnpm db:migrate     # Supabase マイグレーション
```

PRを出す前に `pnpm typecheck && pnpm test && pnpm lint` が通っていることを確認してください。
